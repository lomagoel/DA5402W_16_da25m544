#name of pipeline
name: deploy ML training

on:
  push:
    branches:
      - main
    paths:
      - src/*
        scripts/*
        training_dataset.dvc
        
env:
  SA_KEY: ${{secrets.SA_KEY}}
  ARTIFACT_REPO_REGION: ${{vars.ARTIFACT_REPO_REGION}}
  PROJECT_ID: ${{secrets.PROJECT_ID}}
  ARTIFACT_REGISTRY_REPO_NAME: ${{vars.ARTIFACT_REGISTRY_REPO_NAME}}
  IMAGE_NAME: caltech-mobilenetv2
  IMAGE_URI: "${{vars.ARTIFACT_REPO_REGION}}-docker-pkg-dev/${{env.PROJECT_ID}}/${{env.ARTIFACT_REGISTRY_REPO_NAME}}/${{env.IMAGE_NAME}}:{{github.sha}}"


jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: download code
        uses: actions/checkout@v4

      - name: install docker
        run: apt-get install docker
      
      - name: gcloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: build
        run:  docker build -t ${{env.IMAGE_URI}} 

      - name: authenticate using SA
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.SA_KEY }}
      - name: configure docker auth mechanism
        run: gcloud auth configure-docker "${{ARTIFACT_REPO_REGION}}-docker-pkg-dev"

      - name: push to artifact registry
        run:  docker push -t $IMAGE_URI